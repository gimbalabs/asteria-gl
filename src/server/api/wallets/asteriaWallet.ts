/*import { MeshWallet } from "@meshsdk/core";
import { maestroProvider } from "~/server/provider/maestroProvider";
import { getEnv } from "../routers/admin/createAsteria";


export const getSigningKey = () : string => {
    return getEnv("SIGNING_KEY")
}


const asteriaSigningKey = getSigningKey()


export const asteriaWallet = new MeshWallet({
    networkId: 0,
    fetcher: maestroProvider,
    submitter: maestroProvider,
    key: {
      type: "cli",
      payment: asteriaSigningKey,
    },
  });*/
